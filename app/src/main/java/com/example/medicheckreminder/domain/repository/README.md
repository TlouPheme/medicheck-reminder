# Domain Repository Interface Package

Contains the contract (interfaces) for data operations.

Defining these here allows the domain layer to remain independent of data source implementations (Clean Architecture dependency rule).
